A sleek animated button built with HTML and CSS, featuring a glowing gradient border that gives off a modern, neon-style look. 
The design uses pseudo-elements and keyframe animation to create a continuous, smooth color flow around the button.



What It Does
Displays a centered button with a glowing, animated border.
Uses layered pseudo-elements (::before and ::after) to simulate depth and glow.
Incorporates a vibrant, looping gradient for a dynamic lighting effect.
Fully responsive and built with pure CSS — no JavaScript required.
